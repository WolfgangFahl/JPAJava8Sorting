CREATE TABLE folder (ID BIGINT AUTO_INCREMENT NOT NULL, DOCUMENTS LONGBLOB, PRIMARY KEY (ID))
CREATE TABLE document (ID BIGINT AUTO_INCREMENT NOT NULL, NAME VARCHAR(255), PARENTFOLDER_ID BIGINT, DTYPE VARCHAR(31), PRIMARY KEY (ID))

CREATE TABLE folder1 (name VARCHAR(255) NOT NULL, PRIMARY KEY (name))
CREATE TABLE folder (NAME VARCHAR(255) NOT NULL, PRIMARY KEY (NAME))
CREATE TABLE document (NAME VARCHAR(255) NOT NULL, PARENTFOLDER_NAME VARCHAR(255), PRIMARY KEY (NAME))
CREATE TABLE document1 (NAME VARCHAR(255) NOT NULL, PARENTFOLDER_name VARCHAR(255), PRIMARY KEY (NAME))
ALTER TABLE document ADD CONSTRAINT FK_document_PARENTFOLDER_NAME FOREIGN KEY (PARENTFOLDER_NAME) REFERENCES folder (NAME)
ALTER TABLE document1 ADD CONSTRAINT FK_document1_PARENTFOLDER_name FOREIGN KEY (PARENTFOLDER_name) REFERENCES folder1 (name)
